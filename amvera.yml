meta:
  environment: node
  toolchain:
    name: npm
    version: "20"

build:
  additionalCommands: npm install sqlite3 --build-from-source

run:
  scriptName: start
  persistenceMount: /data
  containerPort: 3000
