meta:
  environment: node
  toolchain:
    name: npm
    version: "20"

build:
  additionalCommands: npm rebuild sqlite3 --build-from-source

run:
  command: npm run start
  persistenceMount: /data
  containerPort: 3000
